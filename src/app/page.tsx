import ImageGridTool from "@/components/image-grid-tool";

export default function Home() {
  return (
    <main className="min-h-screen text-foreground" style={{ backgroundColor: 'var(--bg-secondary)' }}>
      <ImageGridTool />
    </main>
  );
}
